<nav class="col-md-3 col-lg-2 d-md-block bg-light sidebar pb-4">
  <div class="position-sticky pt-3">
    <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
      <span>Recent Conversations</span>
    </h6>
    <ul class="nav flex-column" id="conversations_list">
      <%= render 'conversations/conversations', conversations: conversations, user: user %>
    </ul>
    <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
      <span>All Users</span>
    </h6>
    <ul class="nav flex-column">
      <% users.each do |user| %>
        <li class="nav-item">
          <%= link_to user.email, conversations_path(user_id: user), method: :post, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>